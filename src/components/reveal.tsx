"use client"

import { useEffect, useRef, useState } from "react"
type Props = { children: React.ReactNode; from?: "up" | "left" | "right"; delay?: number; className?: string }
const offsets = { up: "translateY(40px)", left: "translateX(-48px)", right: "translateX(48px)" }
export function Reveal({ children, from = "up", delay = 0, className }: Props) { const ref = useRef<HTMLDivElement>(null); const reduced = typeof window !== "undefined" && window.matchMedia("(prefers-reduced-motion: reduce)").matches; const [shown, setShown] = useState(reduced); useEffect(() => { const element = ref.current; if (!element || reduced || typeof IntersectionObserver === "undefined") return; const observer = new IntersectionObserver(([entry]) => setShown(entry.isIntersecting), { threshold: 0.15, rootMargin: "0px 0px -8% 0px" }); observer.observe(element); return () => observer.disconnect() }, [reduced]); return <div ref={ref} className={className} style={{ opacity: shown ? 1 : 0, transform: shown ? "none" : offsets[from], transition: reduced ? "none" : "opacity .7s cubic-bezier(.22,1,.36,1), transform .7s cubic-bezier(.22,1,.36,1)", transitionDelay: `${delay}ms`, willChange: "opacity, transform" }}>{children}</div> }
