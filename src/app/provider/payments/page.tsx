import { prisma } from "@/lib/prisma"
import { requireRole } from "@/lib/session"
import { PaymentsClient } from "./payments-client"

export default async function ProviderPaymentsPage() { const user = await requireRole("PROVIDER"); const provider = await prisma.provider.findUniqueOrThrow({ where: { userId: user.id }, select: { id: true } }); const [bookings, account] = await Promise.all([prisma.booking.findMany({ where: { providerId: provider.id, amount: { not: null } }, orderBy: { createdAt: "desc" }, take: 100, include: { customer: { select: { name: true } } } }), prisma.user.findUnique({ where: { id: user.id }, select: { walletBalance: true } })]); return <PaymentsClient balance={account?.walletBalance ?? 0} bookings={bookings.map((b) => ({ id: b.id, customerName: b.customer.name, amount: b.amount!, status: b.paymentStatus, method: b.paymentMethod, reference: b.paymentReference, date: b.scheduledAt.toISOString(), platformFee: b.platformFee, providerPayout: b.providerPayout, settled: b.payoutAt != null }))} /> }
